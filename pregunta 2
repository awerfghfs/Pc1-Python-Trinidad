# Se solicita al usuario el costo de la comida y el porcentaje de propina
costo_comida = float(input("Ingrese el costo de su comida: $"))
porcentaje_propina = float(input("Ingrese el porcentaje de propina que desea dejar: "))

# Calcular la cantidad de propina
propina = costo_comida * (porcentaje_propina / 100)

# La cantidad de propina a dejar
print("Debería dejar una propina de: ${:.2f}", (propina))
